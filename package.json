{
  "name": "serial-ioc",
  "version": "1.0.0",
  "description": "dependency injection with automatic serial conversion for async objects/functions",
  "main": "serialIOC.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "protractor": "^5.4.1",
    "request-promise": "^4.2.2",
    "webdriver-manager": "^12.1.0"
  },
  "devDependencies": {
    "jasmine": "^3.2.0"
  }
}
